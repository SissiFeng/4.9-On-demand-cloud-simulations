import gradio as gr
import joblib

# TODO: Load the trained model
# model = joblib.load('material_property_model.joblib')

def predict_property(feature1, feature2, feature3):
    # TODO: Make prediction using the model
    # prediction = model.predict([[feature1, feature2, feature3]])
    # return prediction[0]

# TODO: Create Gradio interface
# iface = gr.Interface(
#     fn=predict_property,
#     inputs=[
#         gr.Number(label="Feature 1"),
#         gr.Number(label="Feature 2"),
#         gr.Number(label="Feature 3")
#     ],
#     outputs=gr.Number(label="Predicted Property")
# )

# iface.launch()
